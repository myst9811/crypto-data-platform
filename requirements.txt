# Crypto Data Platform - Modular Requirements
#
# RECOMMENDED: Install only what you need
#
# For Windows users (API + Dashboard only):
#   pip install -r requirements/serving.txt
#
# For specific components:
#   pip install -r requirements/base.txt       # Core utilities
#   pip install -r requirements/api.txt        # FastAPI server
#   pip install -r requirements/dashboard.txt  # Streamlit dashboard
#   pip install -r requirements/ingestion.txt  # Kafka + WebSocket
#   pip install -r requirements/spark.txt      # PySpark (WSL2/Docker recommended)
#   pip install -r requirements/airflow.txt    # Airflow (WSL2/Docker recommended)
#   pip install -r requirements/dev.txt        # Testing tools
#
# For full installation (Linux/WSL2/Docker):
#   pip install -r requirements/all.txt
#
# ============================================================

# Default: Serving layer only (Windows-friendly)
-r requirements/serving.txt
-r requirements/dev.txt
